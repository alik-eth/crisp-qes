# Node / pnpm
node_modules/
.pnpm-store/
*.log
.DS_Store
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Foundry
packages/contracts/cache/
packages/contracts/out/
packages/contracts/broadcast/
packages/contracts/.openzeppelin/

# Noir / Barretenberg
packages/circuit/target/
packages/circuit/proofs/
packages/circuit/Prover.toml
packages/circuit/Verifier.toml

# Fixtures — legally sensitive Diia identity material, never commit
fixtures/diia/

# Editor
.vscode/
.idea/
*.swp

# Local agent / tool state
.claude/
.playwright-mcp/

# Ad-hoc screenshots taken during reviews
review-*.png
live-*.png
# Root-level marketing / landing / about screenshots (don't dump in repo root)
/about-*.png
/fhe-*.png
/landing-*.png

# Bench dir local install
bench/node_modules/
bench/package-lock.json

# Parked vendored experiments — untracked nested checkouts (PQ/longfellow track)
vendor/longfellow-rs/
vendor/longfellow-zk/

# generated v3 vote-worker bundle (vite.voteworker.config.ts)
packages/web/public/vote/

# local CRS mirror for in-browser proving (large; fetched from crs.aztec.network)
packages/web/public/crs/
# captured vote witness fixture (committee-key-specific, ephemeral)
packages/web/public/vote-witness.json
